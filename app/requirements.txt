# Runtime dependencies for the live demo. Smaller than bench/requirements.txt —
# no sentence-transformers (training-only), no implicit (training-only), no
# matplotlib (training-only).
fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
numpy==1.26.4
pandas==2.2.2
torch==2.2.2
faiss-cpu==1.8.0
